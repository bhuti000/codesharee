# ============================================
# CodeShare Backend Environment Variables
# ============================================

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/codeshare

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/codeshare?retryWrites=true&w=majority

# JWT Configuration
# Generate a secure random string (min 32 characters)
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_jwt_key_min_32_characters_long

# Google OAuth Configuration
# Get these from: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# OAuth Callback URL
# For development:
CALLBACK_URL=http://localhost:5000/api/auth/google/callback

# For production:
# CALLBACK_URL=https://yourdomain.com/api/auth/google/callback

# ============================================
# Setup Instructions:
# ============================================
# 1. Copy this file to .env
# 2. Fill in your actual values
# 3. For MongoDB:
#    - Local: Install MongoDB and start it
#    - Atlas: Create account at mongodb.com/cloud/atlas
# 4. For Google OAuth:
#    - Go to https://console.cloud.google.com/
#    - Create a new project
#    - Enable Google+ API
#    - Create OAuth 2.0 credentials (Web application)
#    - Add authorized redirect URIs
#    - Copy Client ID and Secret
# 5. Generate JWT_SECRET using the command above
# 6. Start the server: npm start
